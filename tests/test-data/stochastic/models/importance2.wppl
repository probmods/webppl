var model = function() {
  var x = sample(bernoulliERP, [.5], {
    guide: [bernoulliERP, [.2]]
  });
  var y = sample(bernoulliERP, [.5], {
    guide: [bernoulliERP, [.7]]
  });
  factor((x || y) ? 0 : -Infinity);
  return [x, y];
};
