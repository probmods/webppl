var model = function() {
  var x = uniform(0, 1);
  var marginal = Enumerate(function() {
    var z = flip(x);
    factor(!z ? 0 : -2);
    return z;
  });
  factor(score(marginal, true));
  return x;
};
