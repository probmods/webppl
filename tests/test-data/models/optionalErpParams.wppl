var makeCoin = function(p) {
  return Enumerate(function() {return flip(p);});
};

var FairCoin = makeCoin(.5);

var model = function() {
  var x = flip(0.5);
  sample(FairCoin, x ? [0] : undefined);
  sample(FairCoin, x ? undefined : [0]);
  return 1;
};
