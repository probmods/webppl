Rejection(
  function() {
    var x = flip(0.5);
    factor(x ? 1 : 0);
    var y = flip(0.5);
    factor((x | y) ? 0 : -Infinity);
    factor(x ? -1 : 0);
    return x;
  },
  5000, // samples
  1)    // upper bound on score
