var erpA = categoricalERP([0.2, 0.3, 0.4, 0.1], [1, 2, 9, 10]);
var lifter = function(s) { return s > 5 ? 'B' : 'A'; }
var erpB = liftERP(erpA, lifter);

var model = function() {
  var e = sample(erpB);
  return e;
};
