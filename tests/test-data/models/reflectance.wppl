var model = function() {
  var reflectance = gaussian(1, 1);
  var illuminance = gaussian(3, 0.5);
  var lightness = reflectance * illuminance;
  factor(gaussianERP.score([5, 0.1], lightness))
  return reflectance
};
